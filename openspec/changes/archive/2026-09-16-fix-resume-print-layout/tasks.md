## 1. CSS Modifications

- [x] 1.1 Adicionar `@media print` no `src/app/globals.css` ocultando todos os elementos filhos do body e zerando as margens padrão do browser (verificar na UI do navegador via print preview que cabeçalhos sumiram e a tela ficou branca inicialmente).
- [x] 1.2 Configurar `.resume-container` com `visibility: visible` e posicionamento absoluto no topo dentro das regras de `@media print` (verificar na UI via print preview que o currículo é exibido com margens amigáveis de documento PDF).
