## 1. Frontend Updates

- [x] 1.1 Update `src/components/kanban-board.tsx` to include `"DROPPED"` in the `KANBAN_COLUMNS` array and verify the new column renders in the UI.
- [x] 1.2 Verify that a job application card can be dragged into the "DROPPED" column and the state persists.
